const add=(a,b)=>{
    return a+b
}

add(1,2)

const foo = () => {
    return 0;
};

const bar = () => {
    return {
       bar: {
            foo: 1,
            bar: 2,
        }
    };
};
console.log(foo);
console.log(bar);

